"""tinytorch.viz — computational-graph visualiser.

    M3 — ``draw_graph(root)``: a graphviz ``Digraph`` that labels operation nodes
    by ``Value._op`` and shows ``data`` / ``grad`` for every node.

This is tooling (Claude may write it), not part of the autodiff lesson.
Stub — implemented in M3.
"""

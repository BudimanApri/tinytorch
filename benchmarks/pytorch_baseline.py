"""M7 — equivalent MLP in PyTorch, for an apples-to-apples comparison against
tinytorch on MNIST (accuracy parity + the honest speed gap).

Requires ``requirements-bench.txt`` (torch). Stub — implemented in M7.
"""

"""M4 — train a scalar-engine MLP on the sklearn two-moons toy dataset.

Wires the canonical loop the builder must be able to explain:
``zero_grad -> forward -> loss -> backward -> step``, with hand-rolled SGD, then
plots the decision boundary. Stub — implemented in M4.
"""
